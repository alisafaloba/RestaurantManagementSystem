spring.application.name=RestaurantManagementSystem
# REQUIRED: Database URL, including port and database name (e.g., restaurant_db)
spring.datasource.url=jdbc:mysql://localhost:3306/restaurant_db?serverTimezone=UTC

# REQUIRED: Your MySQL username (usually 'root' for local)
spring.datasource.username=root

# REQUIRED: Your MySQL password
spring.datasource.password=Alismar@2004

# REQUIRED: Driver Class Name (Confirms MySQL is used)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# RECOMMENDED: JPA/Hibernate settings for automatic table generation
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
